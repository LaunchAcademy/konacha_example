<p id="notice"><%= notice %></p>

<h1><%= @post.title %></h1>

<%= simple_format @post.body %>

<h3>Comments</h3>

<%= link_to 'Add a comment', class: 'comment-form-trigger' %>

<div id="comment-form">
  <%= render 'comments/form', comment: @comment || @post.comments.new %>
</div>
